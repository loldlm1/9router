# 9Router environment contract
# This file reflects actual runtime usage in the current codebase.

# Required
JWT_SECRET=change-me-to-a-long-random-secret
INITIAL_PASSWORD=change-me
DATA_DIR=/var/lib/9router

# Recommended runtime variables
PORT=20128
NODE_ENV=production

# Recommended security and ops variables
API_KEY_SECRET=endpoint-proxy-api-key-secret
MACHINE_ID_SALT=endpoint-proxy-salt
ENABLE_REQUEST_LOGS=false
OBSERVABILITY_ENABLED=true
AUTH_COOKIE_SECURE=false
REQUIRE_API_KEY=false

# Cloud sync variables
# Must point to this running instance so internal sync jobs can call /api/sync/cloud.
# Server-side preferred variables:
BASE_URL=http://localhost:20128
CLOUD_URL=https://9router.com
# Backward-compatible/public variables:
NEXT_PUBLIC_BASE_URL=http://localhost:20128
NEXT_PUBLIC_CLOUD_URL=https://9router.com

# Optional outbound proxy variables for upstream provider calls
# Lowercase variants are also supported: http_proxy, https_proxy, all_proxy, no_proxy
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
# ALL_PROXY=socks5://127.0.0.1:7890
# NO_PROXY=localhost,127.0.0.1

# Optional SearXNG endpoint for the built-in unauthenticated web-search provider.
# SEARXNG_URL=http://searxng:8080/search

# Codex Responses startup: end preflight at a normal event, deadline, or byte cap.
# Finite ranges: deadline 1..10000 ms; prefix 1024..1048576 bytes.
# CODEX_SSE_PEEK_TIMEOUT_MS=1000
# CODEX_SSE_PEEK_BYTES=262144
# RESPONSES_MAX_EVENT_BYTES=16777216

# Codex liveness (milliseconds). The router read-idle timeout falls back to
# STREAM_STALL_TIMEOUT_MS (360000). Heartbeat zero disables comments.
# CODEX_STREAM_STALL_TIMEOUT_MS=360000
# CODEX_STREAM_HEARTBEAT_MS=15000
# CODEX_FETCH_CONNECT_TIMEOUT_MS=10000
# FETCH_CONNECT_TIMEOUT_MS=60000
# Body timeout is clamped to at least the Codex router idle budget + 30000 ms.
# CODEX_FETCH_BODY_TIMEOUT_MS=390000

# Currently unused by application runtime (kept as reference)
# INSTANCE_NAME=9router
